- name: Executar comando hostname nos servidores
  hosts: all
  gather_facts: no
  tasks:
    - name: Executar hostname
      command: cd /u01/app/oracle , ls -lR *
      register: saida

    - name: Exibir o resultado
      debug:
        msg: "O hostname remoto é: {{ saida.stdout }}"
        
