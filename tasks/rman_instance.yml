---
- name: Definir variáveis da instância
  set_fact:
    oracle_sid: "{{ db.sid }}"
    instance_base_dir: "{{ base_dir }}/{{ db.sid | lower }}"
    log_dir: "{{ base_dir }}/{{ db.sid | lower }}/logs"

# =========================================================
# GARANTIR DIRETÓRIOS
# =========================================================

- name: Garantir diretório de logs
  shell: |
    mkdir -p "{{ log_dir }}"
  args:
    executable: /bin/bash
  changed_when: false

- name: Definir timestamp
  set_fact:
    DATA: "{{ lookup('pipe','date +%Y%m%d_%H%M') }}"

# =========================================================
# ARCHIVELOG
# =========================================================

- block:

    - name: Criar cmdfile ARCHIVELOG
      shell: |
        cat > /tmp/rman_arch_{{ oracle_sid }}.rcv <<EOF
RUN {
  allocate channel d1 type disk format '{{ instance_base_dir }}/bkp_arc_%d_%U.rman' MAXPIECESIZE 64G;
  allocate channel d2 type disk format '{{ instance_base_dir }}/bkp_arc_%d_%U.rman' MAXPIECESIZE 64G;

  BACKUP ARCHIVELOG ALL DELETE ALL INPUT TAG 'ARCH_BACKUP';

  release channel d1;
  release channel d2;
}
exit;
EOF
      args:
        executable: /bin/bash

    - name: Executar backup ARCHIVELOG
      shell: |
        source ~/.bash_profile

        export ORACLE_SID={{ oracle_sid }}
        export ORACLE_HOME={{ oracle_home }}
        export PATH=$ORACLE_HOME/bin:$PATH

        LOG="{{ log_dir }}/backup_archivelog_{{ oracle_sid }}_{{ DATA }}.log"

        $ORACLE_HOME/bin/rman \
          target / \
          cmdfile=/tmp/rman_arch_{{ oracle_sid }}.rcv \
          log=${LOG}

      args:
        executable: /bin/bash

      register: arch_exec
      retries: 2
      delay: 30
      until: arch_exec.rc == 0

    - name: Validar log ARCHIVELOG
      shell: |
        LOG="{{ log_dir }}/backup_archivelog_{{ oracle_sid }}_{{ DATA }}.log"

        if [ ! -f "$LOG" ]; then
          echo "LOG_NOT_FOUND"
          exit 0
        fi

        grep -E "RMAN-00569|RMAN-00571|RMAN-03002|ORA-" "$LOG" \
        | grep -v -E "RMAN-08137|RMAN-08120|RMAN-08591|ORA-19606" \
        || true

      args:
        executable: /bin/bash

      register: check_arch

    - name: Falhar se erro ARCHIVELOG
      fail:
        msg: "Erro RMAN ARCHIVELOG SID {{ oracle_sid }}: {{ check_arch.stdout }}"
      when:
        - check_arch.stdout not in ["", "LOG_NOT_FOUND"]

  when: backup_type == "archivelog"

# =========================================================
# FULL
# =========================================================

- block:

    - name: Criar cmdfile FULL
      shell: |
        cat > /tmp/rman_full_{{ oracle_sid }}.rcv <<EOF
RUN {
  allocate channel d1 type disk format '{{ instance_base_dir }}/bkp_full_%d_%U.rman' MAXPIECESIZE 64G;
  allocate channel d2 type disk format '{{ instance_base_dir }}/bkp_full_%d_%U.rman' MAXPIECESIZE 64G;

  BACKUP AS COMPRESSED BACKUPSET
  INCREMENTAL LEVEL 0
  DATABASE
  PLUS ARCHIVELOG
  TAG 'FULL_BACKUP';

  BACKUP CURRENT CONTROLFILE TAG 'CONTROLFILE_BACKUP';

  BACKUP SPFILE TAG 'SPFILE_BACKUP';

  release channel d1;
  release channel d2;
}
exit;
EOF
      args:
        executable: /bin/bash

    - name: Executar backup FULL
      shell: |
        source ~/.bash_profile

        export ORACLE_SID={{ oracle_sid }}
        export ORACLE_HOME={{ oracle_home }}
        export PATH=$ORACLE_HOME/bin:$PATH

        LOG="{{ log_dir }}/backup_full_{{ oracle_sid }}_{{ DATA }}.log"

        $ORACLE_HOME/bin/rman \
          target / \
          cmdfile=/tmp/rman_full_{{ oracle_sid }}.rcv \
          log=${LOG}

      args:
        executable: /bin/bash

      register: full_exec
      retries: 2
      delay: 30
      until: full_exec.rc == 0

    - name: Validar log FULL
      shell: |
        LOG="{{ log_dir }}/backup_full_{{ oracle_sid }}_{{ DATA }}.log"

        if [ ! -f "$LOG" ]; then
          echo "LOG_NOT_FOUND"
          exit 0
        fi

        grep -E "RMAN-00569|RMAN-00571|RMAN-03002|ORA-" "$LOG" \
        | grep -v -E "RMAN-08137|RMAN-08120|RMAN-08591|ORA-19606" \
        || true

      args:
        executable: /bin/bash

      register: check_full

    - name: Falhar se erro FULL
      fail:
        msg: "Erro RMAN FULL SID {{ oracle_sid }}: {{ check_full.stdout }}"
      when:
        - check_full.stdout not in ["", "LOG_NOT_FOUND"]

  when: backup_type == "full"

# =========================================================
# CLEANUP
# =========================================================

- block:

    - name: Criar cmdfile CLEANUP
      shell: |
        cat > /tmp/rman_cleanup_{{ oracle_sid }}.rcv <<EOF
RUN {
  ALLOCATE CHANNEL c1 DEVICE TYPE DISK;

  CONFIGURE RETENTION POLICY TO RECOVERY WINDOW OF 30 DAYS;

  CROSSCHECK ARCHIVELOG ALL;
  DELETE NOPROMPT EXPIRED ARCHIVELOG ALL;

  CROSSCHECK BACKUP;
  DELETE NOPROMPT EXPIRED BACKUP;

  DELETE NOPROMPT OBSOLETE DEVICE TYPE DISK;

  RELEASE CHANNEL c1;
}
exit;
EOF
      args:
        executable: /bin/bash

    - name: Executar cleanup RMAN
      shell: |
        source ~/.bash_profile

        export ORACLE_SID={{ oracle_sid }}
        export ORACLE_HOME={{ oracle_home }}
        export PATH=$ORACLE_HOME/bin:$PATH

        LOG="{{ log_dir }}/cleanup_{{ oracle_sid }}_{{ DATA }}.log"

        $ORACLE_HOME/bin/rman \
          target / \
          cmdfile=/tmp/rman_cleanup_{{ oracle_sid }}.rcv \
          log=${LOG}

      args:
        executable: /bin/bash

      register: cleanup_exec
      retries: 2
      delay: 30
      until: cleanup_exec.rc == 0

    - name: Validar log CLEANUP
      shell: |
        LOG="{{ log_dir }}/cleanup_{{ oracle_sid }}_{{ DATA }}.log"

        if [ ! -f "$LOG" ]; then
          echo "LOG_NOT_FOUND"
          exit 0
        fi

        grep -E "RMAN-00569|RMAN-00571|RMAN-03002|ORA-" "$LOG" \
        | grep -v -E "RMAN-08137|RMAN-08120|RMAN-08591|ORA-19606" \
        || true

      args:
        executable: /bin/bash

      register: check_cleanup

    - name: Falhar se erro CLEANUP
      fail:
        msg: "Erro RMAN CLEANUP SID {{ oracle_sid }}: {{ check_cleanup.stdout }}"
      when:
        - check_cleanup.stdout not in ["", "LOG_NOT_FOUND"]

  when: backup_type == "cleanup"

# =========================================================
# RESULTADO
# =========================================================

- name: Registrar resultado RMAN
  set_fact:
    rman_result: "{{ (rman_result | default([])) + [ {
      'host': inventory_hostname,
      'sid': oracle_sid,
      'backup_type': backup_type,
      'status': 'SUCCESS',
      'status_class': 'success',
      'log': log_dir
    } ] }}"
