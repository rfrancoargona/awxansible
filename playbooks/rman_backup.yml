---
- name: Oracle RMAN Enterprise Backup
  hosts: all

  gather_facts: no

  become: yes
  become_method: sudo
  become_user: oracle

  vars:
    backup_type: "{{ tipo_backup }}"

  tasks:

    - include_tasks: tasks/rman_instance.yml
